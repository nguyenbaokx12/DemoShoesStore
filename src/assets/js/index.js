(function () {
    $('.slick-slider').slick({
        slidesToShow: 6, // Hiển thị 3 ảnh cùng lúc
        slidesToScroll: 1, // Trượt 1 ảnh mỗi lần
        infinite: true, // Trượt vô hạn
        autoplay: true, // Tự động trượt
        autoplaySpeed: 1000, // Tốc độ tự động trượt
    });
})();


